import { useState } from 'react';
import { Award, Calendar, Users, Sparkles, Building2 } from 'lucide-react';
import previousSpeakers from "@/data/previousSpeakers";

const PreviousSpeakersSection = () => {
  const [hoveredIndex, setHoveredIndex] = useState(null);

  return (
    <section className="py-16 bg-gradient-to-br from-emerald-50/50 via-amber-50/30 to-green-50/40 relative overflow-hidden">
      
      {/* Decorative Background Elements */}
      <div className="absolute inset-0 pointer-events-none opacity-[0.04]">
        <div className="absolute top-16 left-8 text-emerald-600 text-6xl">📚</div>
        <div className="absolute bottom-24 right-16 text-amber-600 text-5xl">🎓</div>
        <div className="absolute top-1/2 right-8 text-green-600 text-4xl">⭐</div>
        <div className="absolute bottom-16 left-16 text-emerald-500 text-5xl">🏆</div>
      </div>

      {/* Subtle Pattern Overlay */}
      <div className="absolute inset-0 pointer-events-none opacity-[0.015]">
        <div className="absolute inset-0" style={{
          backgroundImage: 'radial-gradient(circle at 2px 2px, #059669 1px, transparent 0)',
          backgroundSize: '50px 50px'
        }}></div>
      </div>

      <div className="container mx-auto px-4 max-w-7xl relative z-10">
        
        {/* Header Section */}
        <div className="text-center mb-12 space-y-3">
          
          {/* Badge */}
          <div className="inline-flex items-center gap-2 px-4 py-1.5 bg-gradient-to-r from-emerald-100/80 to-amber-100/80 rounded-full border border-emerald-200/50 shadow-sm">
            <Calendar size={14} className="text-emerald-600" />
            <span className="text-xs font-bold text-emerald-700 tracking-wider uppercase">Legacy Speakers</span>
          </div>

          {/* Title */}
          <div className="space-y-2">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-800 leading-tight">
              PREVIOUS YEARS <span className="text-transparent bg-clip-text bg-gradient-to-r from-emerald-600 via-amber-600 to-emerald-600">SPEAKERS</span>
            </h2>
            <p className="text-base text-gray-600 max-w-2xl mx-auto font-light">
              Honoring the distinguished experts who have graced our events over the years
            </p>
          </div>

          {/* Stats Bar */}
          <div className="flex justify-center gap-6 flex-wrap pt-3">
            <div className="flex items-center gap-2 px-4 py-2 bg-white rounded-xl shadow-md border border-emerald-100/50">
              <div className="bg-emerald-50 p-1.5 rounded-lg">
                <Users size={18} className="text-emerald-600" />
              </div>
              <div className="text-left">
                <div className="text-xs text-gray-500 font-medium">Past Speakers</div>
                <div className="text-base font-bold text-gray-900">{previousSpeakers.length}+</div>
              </div>
            </div>
            <div className="flex items-center gap-2 px-4 py-2 bg-white rounded-xl shadow-md border border-amber-100/50">
              <div className="bg-amber-50 p-1.5 rounded-lg">
                <Award size={18} className="text-amber-600" />
              </div>
              <div className="text-left">
                <div className="text-xs text-gray-500 font-medium">Years of Excellence</div>
                <div className="text-base font-bold text-gray-900">5+ Years</div>
              </div>
            </div>
          </div>
        </div>

        {/* Premium Card Grid */}
        <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-5">
          {previousSpeakers.map((sp, index) => (
            <div
              key={index}
              onMouseEnter={() => setHoveredIndex(index)}
              onMouseLeave={() => setHoveredIndex(null)}
              className="group bg-white rounded-2xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-500 border-2 border-emerald-100/40 hover:border-amber-300/70"
            >
              {/* Image Container */}
              <div className="relative h-44 overflow-hidden bg-gradient-to-br from-emerald-50/30 to-amber-50/30">
                
                {/* Hover Overlay with Gradient */}
                <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-black/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>
                
                {/* Top Corner Sparkle */}
                <div className="absolute top-2 right-2 z-20 opacity-0 group-hover:opacity-100 transition-all duration-500 transform group-hover:rotate-12">
                  <div className="bg-white/90 backdrop-blur-sm p-1.5 rounded-full shadow-lg">
                    <Sparkles size={12} className="text-amber-600" />
                  </div>
                </div>

                {/* Image */}
                <img
                  src={sp.image}
                  alt={sp.name}
                  className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
                />

                {/* Bottom Accent on Hover */}
                <div className="absolute bottom-0 left-0 right-0 h-1 bg-gradient-to-r from-emerald-400 via-amber-400 to-emerald-400 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
              </div>

              {/* Info Section */}
              <div className="p-3.5 bg-gradient-to-br from-white to-emerald-50/10 relative">
                
                {/* Top Border Accent */}
                <div className="absolute top-0 left-0 right-0 h-0.5 bg-gradient-to-r from-emerald-400 via-amber-400 to-emerald-400"></div>

                <div className="space-y-1.5">
                  {/* Name */}
                  <h3 className="font-bold text-sm leading-tight group-hover:text-emerald-700 transition-colors duration-300" style={{ color: '#d9a520' }}>
                    {sp.name}
                  </h3>

                  {/* Role with Icon */}
                  <div className="flex items-center gap-1">
                    <Award size={10} className="text-emerald-600 flex-shrink-0" />
                    <p className="text-[11px] font-semibold text-emerald-700 leading-tight">
                      {sp.role}
                    </p>
                  </div>

                  {/* Organization with Icon */}
                  <div className="flex items-center gap-1">
                    <Building2 size={10} className="text-gray-400 flex-shrink-0" />
                    <p className="text-[10px] text-gray-600 leading-tight font-medium">
                      {sp.org}
                    </p>
                  </div>
                </div>

                {/* Subtle Glow Effect */}
                <div className="absolute inset-0 bg-gradient-to-t from-emerald-500/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none"></div>
              </div>

              {/* Card Border Glow */}
              <div 
                className="absolute inset-0 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none"
                style={{
                  boxShadow: '0 0 30px rgba(16, 185, 129, 0.2)'
                }}
              ></div>
            </div>
          ))}
        </div>

        {/* Bottom CTA or Info Section */}
        <div className="mt-12 text-center">
          <div className="inline-block bg-white/80 backdrop-blur-sm rounded-2xl px-8 py-4 border-2 border-emerald-200/50 shadow-lg">
            <div className="flex items-center justify-center gap-3">
              <div className="bg-emerald-50 p-2 rounded-full">
                <Calendar size={20} className="text-emerald-600" />
              </div>
              <div className="text-left">
                <p className="text-xs text-gray-500 font-medium">Grateful for their contributions</p>
                <p className="text-sm font-bold text-gray-900">Building a legacy of knowledge</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
};

export default PreviousSpeakersSection;